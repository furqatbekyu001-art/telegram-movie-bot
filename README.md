# Telegram Movie Bot

Async Telegram movie-code bot with persistent statistics.

Runtime data files (`movies.json` and `stats.json`) are intentionally kept local and ignored from the public repository.
